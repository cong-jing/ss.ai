/**
 * Test suite entry-point — run with: npm test
 *
 * Import order determines execution order.
 * Each imported file registers its own describe/it blocks via node:test.
 */

import "./messages.test.js";
import "./characters.test.js";
import "./conversations.test.js";
